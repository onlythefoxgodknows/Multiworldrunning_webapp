export default class Entity {
    constructor(row, col, width, height, solid) {
        this.row = row;
        this.col = col;
        this.width = width;
        this.height = height;
        this.solid = solid;
    }

    //implement tick method later
    render(ctx, sprite) {

    }
}